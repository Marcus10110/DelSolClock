# Copy to .env (gitignored) and fill in your Mapbox public token (pk.*).
# Used by tools/capture-route.mjs to fetch real Directions responses into
# test/fixtures/. A default public token with no special scopes is sufficient;
# leave URL restrictions OFF for the capture script (it runs in Node, not a browser).
MAPBOX_TOKEN=
